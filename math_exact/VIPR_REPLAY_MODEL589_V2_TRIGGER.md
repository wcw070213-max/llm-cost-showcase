# Model589 VIPR cold replay V2 trigger

The source artifact is bound by its own machine-readable proof SHA. This workflow reconstructs the frozen LP, verifies the proof against the source summary, rebuilds the pinned VIPR checker, and executes a second checker run.
